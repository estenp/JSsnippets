for (i = 1; i <=100; i++) {
    value = '';
    if (i % 3 === 0) {
    	value = value+'Fizz';
    }
    if (i % 5 === 0) {
    	value = value+'Buzz';
    }
    if (value === '') {
    	value = i;
	}
    		document.getElementById('div').insertAdjacentHTML('beforeend', '<div>'+value+'</div>');
}
